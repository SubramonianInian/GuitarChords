export enum lineType {
  unspecified = 0,
  chord = 1,
  lyric = 2
}

export enum displayDataType {
  unspecified = 0,
  tileView = 1,
  chordView = 2
}

export enum instrumentType {
  unspecified = 0,
  keyboard = 1,
  guitar = 2
}
